# LLM Cortex - Dependencies
#
# Core (required for Layers 1-3):
#   No dependencies — uses only Python stdlib
#
# Layer 0 - Observation Pipeline:
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
httpx>=0.27.0
#
# Layer 6 - Knowledge Graph:
networkx>=3.0
#
# Optional - Vector similarity search (Layer 0 + Layer 5):
# sqlite-vec>=0.1.0
# openai>=1.0.0
